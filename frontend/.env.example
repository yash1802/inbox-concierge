# Split dev (Vite HMR on :5173, API on another origin): set to your API base URL (no trailing slash).
# Must match the host users use for the API (same as OAUTH_REDIRECT_URI host in backend .env).
# Leave unset when serving the built UI from FastAPI (single origin).
VITE_API_BASE_URL=http://localhost:8000
